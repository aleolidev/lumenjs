# Minimum TypeScript core completion audit

Audit date: 2026-07-12.

Scope: prove a small importable TypeScript core with two distinct original games
before any npm publication. This audit does not approve publication or promote
First Light's renderer, battle, campaign, persistence adapter, schemas, or
internal modules into public contracts.

| # | Requirement | Authoritative evidence | Result |
| ---: | --- | --- | --- |
| 1 | Strict TypeScript source | `src/core/index.ts`; `tsconfig.package.json`; root strict type check | Proven. |
| 2 | ESM and declarations | `dist-package/index.js` and `index.d.ts` generated by `build:package`; closed root export map | Proven. |
| 3 | Small useful API | `createGame`, isolated state/map reads, synchronous dispatch, save creation and validated restore | Proven. |
| 4 | Minimum gameplay slice | Bounded collision movement, faced localized interaction, structured facts, and authored cross-map transitions | Proven. |
| 5 | Willowbound consumer | Core Node test plus generated web export browser test | Proven. |
| 6 | Tideglass Reach consumer | Distinct source identity/content in the same core Node and generated web export browser tests | Proven. |
| 7 | Save safety | Project/version binding, exact fields, authored locale/message checks, walkable positions, inert cross-project and malformed rejection | Proven. |
| 8 | Clean consumer | Reproducible tarball install; bare `lumenjs` JavaScript import; strict `.mts` compile through packaged declarations | Proven. |
| 9 | Existing product regression | 215 Node tests, 84 portable browser passes with 3 expected GPU skips, production build/Cloudflare check, and headed WebGPU gate | Proven locally. |
| 10 | No premature expansion | Specification excludes renderer, scene, battle, creature, module, I/O, DOM, WebGPU, CLI, and persistence-adapter contracts | Proven by source and export inventory. |

## Two-game evidence

Both games load their own manifest, Tiled maps, Lumen world documents, campaign
document, and English/Spanish catalogs. Each starts from its own project/map
identity, moves through collision-aware cells, receives its own Spanish character
text, enters its own second map, creates a save, mutates state, and restores the
saved snapshot. Tideglass Reach additionally runs as a distinct generated static
web export in every portable browser project rather than borrowing Willowbound's
runtime result.

## Boundary decision

First Light stays an internal specialized consumer. Migrating its campaign state
would require adding dialogue, battle, creatures, generational storage, semantic
scene construction, and WebGPU to a core whose two consumers do not need those
contracts. Keeping First Light green proves coexistence; forcing it through this
entry point would broaden the public surface without two-game evidence.

The achieved boundary is useful but experimental. npm remains unpublished. Any
future publication still needs a fresh candidate audit and explicit owner
approval; this completion audit is technical evidence, not that approval.

## Measured candidate

The clean verifier selected 25 files and produced repeated byte-identical
tarballs with SHA-256
`370004016fc3d523d7a88c8e79857a9d17f9bfa75863c02f1c8f70a58d8af954`.
The production dependency audit reported zero known vulnerabilities. Registry
verification reported signatures for 32 packages and attestations for 14; these
dependency facts do not sign or publish LumenJS itself.
