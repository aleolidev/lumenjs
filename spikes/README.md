# Spikes

These experiments answer specific research questions before LumenJS chooses an
engine architecture. They are deliberately isolated from `src` and are not
public APIs or production implementations.

Each spike must state its question, acceptance criteria, result, and deletion
or promotion decision. A successful experiment validates an approach, not its
current code shape.
